machine:
  services:
    - docker

dependencies:
  pre:
    - docker login -e "alexander.saenen@epitech.eu" -u "pickaguidedockercloud" -p "lucasanstoast6"

  override:
    - npm install mocha supertest chai express config body-parser mongoose

deployment:
  production:
    branch: master
    commands:
      - ssh root@82.223.82.41 "bash -ic deploy-web-production"
  staging:
    branch: dev
    commands:
      - make pull
      - make build
      - make push
      - ssh root@82.223.82.41 "bash -ic deploy-web-staging"
